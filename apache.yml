---
- name: Install Apach
  hosts: all
  become: yes
  tasks:
  - name: Update package manager
    apt:
      update_cache: yes
  - name: Install Apache
    apt:
      name: apache2
      state: present
  - name: Start Apache
    service:
      name: apache2
      state: startede
